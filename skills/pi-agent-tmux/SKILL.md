---
name: pi-agent-tmux
description: Launch and resume Pi sessions through pia. Use whenever an agent needs to start Pi.
---

# pia

Use `pia` instead of invoking `pi` directly:

```sh
pia
pia --continue
pia --session /path/to/session.jsonl
```
